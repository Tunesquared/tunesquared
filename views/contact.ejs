<!DOCTYPE html>
<html>
  <head>
    <title>Tunes² - Terms of service.</title>
    <link rel="stylesheet" type="text/css" href="lib/bootswatch/cyborg/bootstrap.min.css" />
    <link rel="stylesheet" type="text/css" href="//netdna.bootstrapcdn.com/font-awesome/3.2.1/css/font-awesome.css" />

    <link rel="stylesheet" type="text/css" href="css/common.css" />

  <script type="text/javascript">
    var RecaptchaOptions = {
      theme : 'blackglass'
    };
  </script>

  <style>
    /* fixes recaptcha style break */
    iframe {display:none !important;}
  </style>

  </head>
  <body>
    <div class="header">
      <div class="container">
        <a class="navbar-brand" href="/">Tune²</a>
      </div>
    </div>
    <div class="container page">
      <div class="col-10 col-offset-1">
        <h1>Contact <small>We need your feedback!</small></h1>
        <p>
          If you have any suggestion/comment or have encountered some bug, please let us know using the form below.
        </p>
        <hr />
        <p>
          <form class="form-horizontal" role="form" action="/contact" method="post">
            <div class="form-group">
              <label for="email" class="col-sm-2 control-label">Your email</label>
              <div class="col-5">
                <input type="email" class="form-control" id="email" placeholder="Email" name="email" value="<%= email %>">
              </div>
            </div>
            <div class="form-group">
              <label for="message" class="col-sm-2 control-label">Your message</label>
              <div class="col-sm-7">
                <textarea class="form-control" rows="8" id="message" name="message"><%= message %></textarea>
              </div>
            </div>
            <div class="form-group <%= captcha_error ? 'has-error' : '' %>">
              <label class="col-sm-2 control-label">Are you human ?</label>
              <div class="col-sm-7">
                <%- captcha %>
                <% if (captcha_error) { %>
                  <span class="help-block">Wrong captcha, please try again.</span>
                <% } else { %>
                  <span class="help-block">Enter the text in the image to verify that you not a spam robot.</span>
                <% } %>
              </div>
            </div>
            <% if (other_error) { %>
              <div class="alert alert-danger">Something went wrong, please try again.</div>
            <% } %>
            <div class="form-group">
              <div class="col-sm-offset-2 col-sm-10">
                <button type="submit" class="btn btn-primary">Send</button>
              </div>
            </div>
          </form>
        </p>
      </div>
    </div>
    <% include footer %>
  </body>
</html>
